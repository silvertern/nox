buildscript {
	repositories.mavenLocal()
	repositories.jcenter()
	dependencies.add("classpath", "com.silvertern.gradle:nox:+")
}

plugins.apply(nox.Platform)

allprojects { p ->
	p.repositories.add(platformRepo("e46", "./platform"))
}

bundleMappings {

}
